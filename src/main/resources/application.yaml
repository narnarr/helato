spring:
  application:
    name: helato
  profiles:
    active: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/HELATO?serverTimezone=Asia/Seoul
    username: root
  jpa:
    database: mysql
    hibernate:
      ddl-auto: validate

config:
  machine-id: 1
